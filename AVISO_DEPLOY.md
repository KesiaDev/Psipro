# Aviso: Deploy Automático

Após fazer `git push` para `main`, o **Railway** faz deploy automático.

- **Backend** e **Web** redeployam ao detectar novo push no GitHub.
- Não é necessário ação manual no painel do Railway (a menos que queira forçar redeploy).
